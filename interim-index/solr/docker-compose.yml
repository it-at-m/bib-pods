# docker compose up -d
# docker compose down
services:
  solr:
    image: solr
    ports:
      - "8983:8983"
    volumes:
      - data:/var/solr
    command:
      - solr-precreate
      - interim-index

volumes:
  data:
